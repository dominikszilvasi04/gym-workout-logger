name: CI-CD

on:
  workflow_dispatch:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  backend-quality:
    name: Backend formatting, linting, and tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Ruff format check
        run: ruff format --check application tests

      - name: Run Ruff lint check
        run: ruff check application tests

      - name: Run pytest
        run: pytest -q

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage-xml
          path: coverage.xml

  frontend-quality:
    name: Frontend lint, test, coverage, and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Run frontend lint
        run: npm run lint
        working-directory: frontend

      - name: Run frontend tests with coverage
        run: npm run test:coverage
        working-directory: frontend

      - name: Upload frontend test coverage report
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage-report
          path: frontend/coverage

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium webkit
        working-directory: frontend

      - name: Run mobile end-to-end tests
        run: npm run test:e2e:mobile
        working-directory: frontend

      - name: Upload mobile end-to-end traces
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-e2e-traces
          path: frontend/test-results

      - name: Run frontend build
        run: npm run build
        working-directory: frontend

  docker-smoke:
    name: Docker smoke test
    runs-on: ubuntu-latest
    needs: [backend-quality, frontend-quality]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build local Docker image
        run: docker build -t gym-workout-logger:ci .

      - name: Start container for smoke test
        run: |
          docker run -d --name gym-workout-logger-smoke \
            -e APPLICATION_SECRET_KEY=smoke-test-secret \
            -p 5001:5000 \
            gym-workout-logger:ci

      - name: Wait for health endpoint
        run: |
          for attempt in {1..30}; do
            if curl -fsS http://127.0.0.1:5001/health > /tmp/health.json; then
              cat /tmp/health.json
              exit 0
            fi
            sleep 2
          done
          echo "Container did not become healthy in time"
          docker logs gym-workout-logger-smoke
          exit 1

      - name: Stop smoke container
        if: always()
        run: |
          docker rm -f gym-workout-logger-smoke || true

  build:
    name: Build and publish image
    runs-on: ubuntu-latest
    needs: docker-smoke
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/gym-workout-logger:latest
            ghcr.io/${{ github.repository_owner }}/gym-workout-logger:${{ github.sha }}
